# 聚合路由器调研 · FreeLLMAPI

> 核实日期：2026-09-14。验证方式：GitHub 仓库 README 原文抓取 + 页面实时元数据。本文档只写抓到的原文数字，推断与事实分开标注。

---

## 一、一句话定位

**把 one-api 的心智模型直接套上去就成立**：本地起一个统一端点，收编各路免费渠道，对外说同一种协议，请求来了自动选路、限流了自动 failover。区别在于 one-api 是"通用网关"（你自己指定模型、自己配渠道），FreeLLMAPI 是"**免费额度专用网关**"——它内置一份持续更新的免费模型目录，知道谁此刻在免费，替你调度。

## 二、仓库事实（2026-09-14 页面原文）

| 项 | 数值 |
|---|---|
| 仓库 | `tashfeenahmed/freellmapi` |
| Star / Fork | 26.1k / 3.6k |
| Open Issues | 26 |
| 许可证 | MIT |
| 宣称规模 | 34 家提供商、635 个免费模型端点、合计约 7.4B tokens/月 |
| 运行形态 | Node 20+ / Docker / 桌面端 / 树莓派，全部自托管 |
| 官方定性 | 原文标注 "Personal experimentation only"，非生产用途 |

## 三、工作机制（README 原文描述）

1. **Key 存储**：各家 provider key 存本地加密 SQLite，数据不出机器（路由本身是本地进程）。
2. **选择引擎**：每个请求挑"当前最优可用模型"；某家限流自动切下一家。
3. **配额记账**：按 key 追踪用量，主动卡在各家免费层上限下面跑——这是它相对 one-api 最核心的差异功能。
4. **协议面**：除 OpenAI 系（`/v1/chat/completions`、`/v1/responses`、`/v1/embeddings`）外，原生暴露 Anthropic `/v1/messages`、Gemini `/v1beta`，还能模拟 Ollama。**意义**：Claude Code、Gemini CLI 这类锁协议的 agent 可以不改配置直接怼上免费池。
5. **自定义后端**：可挂任意 OpenAI-compatible 端点（原文列了 Ollama、LM Studio、vLLM、llama.cpp）——本地模型可以作为一路 provider 混进池子。
6. **客户端接入**：README 称 Claude Code / Codex CLI / Gemini CLI / Aider / Cline / Roo / OpenCode / Cursor / Zed / JetBrains AI 等十几个 agent 有"一键配置生成器"。

## 四、提供商名单 vs 本项目已核实渠道（交叉验证）

它列出的主力提供商，与《免费Token渠道调研-最终版》逐项对照：

| 提供商 | 本项目核实状态（9-11） | 注意 |
|---|---|---|
| Google（Gemini 免费层） | ✅ 核实 | 但官方静态限额表已下线，限额随账号浮动，路由器里的数字可能过期 |
| OpenRouter `:free` | ✅ 核实 | 与已知规则一致：未充值 50 次/天，充 $10 终身 1000 次/天 |
| Cloudflare Workers AI | ✅ 核实 | 10,000 neurons/天 |
| Cerebras | ⚠️ 半免费 | 已核实的是**注册送 $5**，并非永续免费层；挂进池子等于把赠金当额度烧 |
| Groq | ⚠️ 未能核实 | 官方页抓不到，限额数字用前自查 |
| ModelScope / Z.ai（智谱） | ⚠️ 未能核实 | 同上，国内号需实名 |
| Mistral / Cohere / NVIDIA / HuggingFace / OpenCode Zen | 未纳入本项目核实 | 各自免费条款另查 |
| "另外 22 家" | — | README 未逐一列名 |

**推论（非原文）**：它宣称的 7.4B tokens/月是把各家免费上限乘出来的纸面总量，实际可薅量受我们清单里同样的因素约束——限额变更、抢池、风控。路由器不生产额度，只是额度的调度器。

## 五、目录分发模式（信任点，值得单独记）

- 路由器每天两次从 `freellmapi.co` 拉**签名目录**（signed catalog），模型目录集中在作者服务器。
- 免费安装用**月度快照**：新模型进直播后，免费用户 30 天后才能用上。
- **$19/年 Premium** 只卖"当日目录更新"，路由代码本身保持免费开源。

**风险评估**：MIT 代码可自审，但"今天谁免费"这个核心知识来自作者的中心化服务。目录停更/作者跑路 = 退化成过期快照。对比公益站生态（`ai-welfare-hub` 自动探活 JSON）：两者都是"中心化的情报订阅"，信任成本一样存在，只是这个收费、那个靠爱发电。

## 六、与 one-api / new-api 对照

| 维度 | one-api / new-api | FreeLLMAPI |
|---|---|---|
| 定位 | 通用 API 网关，可建站分发 | 个人本地白嫖调度器 |
| 模型选择 | 你指定模型 → 找渠道 | 它推荐"当下最优免费模型" |
| 免费层感知 | 无，渠道限额自己填 | 内置各家免费限额，按 key 卡上限 |
| 管理界面 | Web 控制台、多用户、计费倍率 | 无建站功能，单用户 |
| 协议转换 | OpenAI 进出为主，兼容各家 | OpenAI + Anthropic + Gemini + Ollama 模拟全原生 |
| 数据流向 | 你连的渠道决定 | 你连的渠道决定（本地部分不外发） |

已熟 one-api 的人上手它零成本：把"手动配渠道"换成"批量填免费 key + 让它自动调度"。

## 七、孙学视角：它在食物链的哪一层

对照《孙学·免费Token的真正流向》三层结构：**它不是补贴方，也不是渠道层，是"散户的执行工具层"**——把散户手里散落的官方免费 key 的调度、记账、防超限自动化。羊毛还是那些羊毛，它不加大，只是让你薅得更满、更不容易被反薅（超限封号）。

两个对应风险：
1. **打满免费池正是各家风控模型最眼熟的流量特征**，自动 failover + 无限额囤积小号是封号套餐。主力 Google/GitHub 账号慎用。
2. **隐私分层**：免费层 ToS 普遍允许数据用于训练。敏感上下文走本地 LM Studio 那一路，云端免费池只放可丢的内容——路由器的"本地 provider 混池"恰好支持这种分流。

## 八、落地路线（如果采用）

1. Docker 起路由，先只挂本地 LM Studio（现有 Qwen3.5-4B）验证链路通。
2. 逐家补 key，优先级按第四节核实状态：OpenRouter → Cloudflare → Google，Cerebras/Groq 限额自查后再挂。
3. agent 端（OpenCode / Claude Code）base_url 统一指到这一个端口，本地 4B 做隐私兜底、云端免费池做质量兜底。
4. 目录过期问题：每月重装同步一次快照即可，不必付费（个人用 30 天延迟无所谓）。

## 九、一句话结论

**值得试，但它改变不了免费 token 经济学，只是把我们清单里的信息自动化执行了一遍**：钥匙仍要自己去各家注册，额度天花板一家没变多。它的真实价值是三点——免费层知识内置、Anthropic/Gemini 原生端点、本地+云端混池调度。
